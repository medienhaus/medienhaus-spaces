import { styled } from 'styled-components';

const Icon = styled.span`
    svg {
        display: block;
        width: var(--icon-size);
        height: var(--icon-size);
    }
`;

export default Icon;
